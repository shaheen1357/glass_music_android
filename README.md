# Music (Android)

Native Kotlin + Jetpack Compose + Media3 local music player — the Android
counterpart to the iOS app. Builds a debug APK on GitHub Actions; install it
directly (Settings → enable "install unknown apps" for your browser/Files).
