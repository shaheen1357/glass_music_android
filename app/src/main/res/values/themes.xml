<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base (light). Compose owns real theming/colors; this is just the initial
         window before Compose draws. Framework parent = no extra dependency. -->
    <style name="Theme.Music" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
